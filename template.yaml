---
server:
  username: scan
  password: 1234qwer
  ip_address: 0.0.0.0
  port_number: 65500
  incoming_dir: ENV:PYFTPD_INCOMING_DIR
# incoming_dir : /tmp
  log_dir: ENV:PYFTPD_LOG_DIR
# log_dir: /tmp
post-processing: 
  doc_dir: ENV:PYFTPD_DOC_DIR
# doc_dir: /tmp